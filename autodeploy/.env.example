# ╔══════════════════════════════════════════════════════════════════╗
# ║  AutoDeployX Environment Configuration                          ║
# ║  Copy this file to .env and fill in your values                 ║
# ║  NEVER commit .env to git!                                       ║
# ╚══════════════════════════════════════════════════════════════════╝

# =============================================
# 🔴 REQUIRED: DATABASE CONFIGURATION
# =============================================
DB_PASSWORD=your_secure_database_password_here
POSTGRES_USER=autodeployx
POSTGRES_DB=autodeployx

# =============================================
# 🔴 REQUIRED: DOCKER HUB CONFIGURATION
# Without this = No images in dashboard
# =============================================
DOCKERHUB_USER=your_dockerhub_username          # 🔴 REQUIRED
DOCKERHUB_REPO=autodeployx                       # 🔴 REQUIRED

# 🟡 OPTIONAL but recommended (avoids rate limits)
DOCKERHUB_TOKEN=dckr_pat_xxxxxxxxxxxx
# How to get: hub.docker.com → Account Settings → Security → New Access Token

# =============================================
# 🔴 REQUIRED: JENKINS CI/CD CONFIGURATION
# Without this = Cannot trigger pipelines
# =============================================
JENKINS_URL=http://localhost:8080
JENKINS_USER=admin
JENKINS_TOKEN=your_jenkins_api_token_here        # 🔴 REQUIRED
JENKINS_JOB_NAME=autodeployx-backend

# How to get Jenkins API token:
# 1. Log into Jenkins → Click your username (top right)
# 2. Configure → API Token section
# 3. Add new Token → Copy it here

# ⚠️ ALSO REQUIRED IN JENKINS UI:
# Jenkins → Manage → Credentials → Add:
#   - ID: dockerhub (username + password/token for DockerHub)
#   - ID: github (SSH key or token for GitHub)

# =============================================
# 🟡 OPTIONAL: KUBERNETES/MINIKUBE CONFIGURATION
# Only needed if you want real kubectl commands
# =============================================
K8S_NAMESPACE=default
K8S_DEPLOYMENT_NAME=autodeployx-app
MINIKUBE_PROFILE=minikube

# =============================================
# ⚙️ FEATURE FLAGS
# =============================================
# Set to 'true' to enable real Jenkins API calls
ENABLE_REAL_JENKINS=false

# Set to 'true' to enable real kubectl commands
# Requires: ~/.kube/config mounted, minikube running
ENABLE_REAL_K8S=false

# =============================================
# 🖥️ FRONTEND CONFIGURATION
# =============================================
VITE_BACKEND_URL=http://localhost:8000
VITE_WS_URL=ws://localhost:8000/ws

# =============================================
# 📋 CREDENTIALS CHECKLIST
# =============================================
# 
# ┌─────────────────┬───────────────────────────────────┬────────────┐
# │ System          │ Credential                        │ Where      │
# ├─────────────────┼───────────────────────────────────┼────────────┤
# │ DockerHub       │ DOCKERHUB_USER + DOCKERHUB_TOKEN │ This .env  │
# │ Jenkins API     │ JENKINS_TOKEN                     │ This .env  │
# │ Jenkins → Docker│ dockerhub credentials ID          │ Jenkins UI │
# │ Jenkins → GitHub│ github credentials ID             │ Jenkins UI │
# │ Kubernetes      │ ~/.kube/config mount              │ docker-compose │
# └─────────────────┴───────────────────────────────────┴────────────┘
#
# Missing = Dashboard shows empty data!
# Check: curl http://localhost:8000/credentials/status
